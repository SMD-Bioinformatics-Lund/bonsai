#!/bin/bash
REPO=clinicalgenomicslund/bonsai
#VERSION="1.0.0"
VERSION="dev"

# build and push
#docker build -t "${REPO}-api:${VERSION}" api && docker push "${REPO}-api:${VERSION}"
#docker build -t "${REPO}-app:${VERSION}" frontend && docker push "${REPO}-app:${VERSION}"
#docker build -t "${REPO}-minhash-clustering:${VERSION}" minhash_service && docker push "${REPO}-minhash-clustering:${VERSION}"
#docker build -t "${REPO}-allele-clustering:${VERSION}" allele_cluster_service && docker push "${REPO}-allele-clustering:${VERSION}"
#docker build -t "${REPO}-ska-clustering:${VERSION}" ska_service && docker push "${REPO}-ska-clustering:${VERSION}"
