# Opt in to the low-thread Conda build path for constrained legacy hosts.
BONSAI_LEGACY_CONDA_BUILD=true

# Avoid building multiple service images concurrently on process-limited hosts.
COMPOSE_PARALLEL_LIMIT=1
